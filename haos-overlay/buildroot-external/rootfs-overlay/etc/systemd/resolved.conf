[Resolve]
# Use public DNS servers directly (bypass potentially unreliable router DNS)
DNS=223.5.5.5 8.8.8.8
FallbackDNS=8.8.8.8 8.8.4.4 1.1.1.1
# Disable DNSOverTLS - some routers/DNS servers don't support it, causing timeouts
DNSOverTLS=no
